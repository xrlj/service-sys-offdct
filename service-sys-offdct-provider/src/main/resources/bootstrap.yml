spring:
  application:
    name: service-sys-offdct
  profiles:
    active: test
# 加上FeignConfiguration后，不加上下面这段，feign无法请求。
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false
        isolation:
          strategy: SEMAPHORE

